import Link from "next/link";
import { companyConfig, WHOP_FEATURES_PARAGRAPH } from "@/config/company";
import { WHOP_FEATURES_PARAGRAPH as feat } from "@/lib/whop";
export default function Home() {
  return (
    <div className="p-8 max-w-3xl mx-auto">
      <h1 className="text-3xl font-bold">{companyConfig.name}</h1>
      <p className="mt-2 text-gray-600">{companyConfig.tagline}</p>
      <p className="mt-1 text-sm">{companyConfig.description}</p>
      <div className="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
        {companyConfig.services.map((s:any)=><div key={s.id} className="border p-4 rounded"><h3 className="font-bold">{s.name}</h3><p className="text-sm">${s.basePrice} - {s.time}</p></div>)}
      </div>
      <div className="mt-8 space-y-3">
        <Link href="/estimate" className="block w-full bg-purple-600 text-white p-3 rounded text-center">Start Free Estimate → Leads API</Link>
        <Link href="/checkout?leadId=demo" className="block w-full border border-purple-600 text-purple-600 p-3 rounded text-center">Demo Checkout - Payment Elements</Link>
      </div>
      <div className="mt-10 p-4 bg-gray-50 rounded text-xs">
        <h3 className="font-bold mb-2">Whop Integration (for Bounty Review):</h3>
        <p>{feat || WHOP_FEATURES_PARAGRAPH}</p>
        <p className="mt-2">Reusable: Change ONE file `src/config/company.ts` to rebrand for any service business (cleaning, plumbing, roofing).</p>
        <p className="mt-2">Pixels visible in: Console + localStorage key `whop_pixel_events` + `whop_pixel_fired` event.</p>
      </div>
    </div>
  );
}
